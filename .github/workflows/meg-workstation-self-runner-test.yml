# .github/workflows/meg-workstation-self-runner-test.yml
name: MEG-Workstation Self-Runner Test

on:
  pull_request:
    types: [opened, synchronize, edited, reopened]  # you can add "reopened" too if needed

jobs:
  run-on-self-hosted:
    if: startsWith(github.event.pull_request.title, 'Process')  # 💡 This filters PRs
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print Message
        run: echo "✅ Running on self-hosted because PR title starts with 'Process'"
